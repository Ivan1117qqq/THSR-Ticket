[pytest]
testpaths = thsr_ticket/unittest
markers =
    integration: reads the live THSR website (requires --live)
    browser: offline browser transport tests (requires --browser-tests and installed Chrome)
